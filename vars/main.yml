#SPDX-License-Identifier: MIT-0
---
# vars file for ansible-role_telegraf
# In this example global group vars are used

urls: "{{ influxdb2_telegraf_urls | map('tojson') | join(', ') }}"
organization: "{{ influxdb2_telegraf_organization }}"
bucket: "{{ influxdb2_telegraf_bucket }}"
token: "{{ influxdb2_telegraf_token }}"
